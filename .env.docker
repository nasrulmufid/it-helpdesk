# IT Help Desk v2.2.0 - Docker Environment Configuration
# Copy this file to .env and update values as needed

# Application Configuration
APP_NAME="IT Help Desk v2.2.0"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:FvfeVzMM9iYe0KxLCfHh6nqNUPsC7XDpqm8N1/y4/9U=
APP_URL=http://localhost:8000
APP_TIMEZONE=Asia/Jakarta

# Localization
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# Database Configuration (MySQL)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=it_helpdesk
DB_USERNAME=helpdesk_user
DB_PASSWORD=helpdesk_pass_2024

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=localhost

# Cache Configuration
CACHE_STORE=database
CACHE_PREFIX=it_helpdesk_cache

# Queue Configuration
QUEUE_CONNECTION=database
QUEUE_RETRY_AFTER=90

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@helpdesk.local
MAIL_FROM_NAME="${APP_NAME}"

# Broadcasting Configuration
BROADCAST_CONNECTION=log

# Logging Configuration
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Security Configuration
SANCTUM_STATEFUL_DOMAINS=localhost:8000,localhost:3000
SESSION_SECURE_COOKIE=false

# Performance Configuration
PHP_MEMORY_LIMIT=256M
PHP_MAX_EXECUTION_TIME=300
PHP_UPLOAD_MAX_FILESIZE=100M
PHP_POST_MAX_SIZE=100M

# Docker Specific Configuration
DOCKER_CONTAINER_NAME=it-helpdesk-app
DOCKER_MYSQL_CONTAINER=it-helpdesk-mysql
DOCKER_PHPMYADMIN_CONTAINER=it-helpdesk-phpmyadmin

# MySQL Root Configuration (for phpMyAdmin)
MYSQL_ROOT_PASSWORD=root_pass_2024_secure
MYSQL_ROOT_HOST=%

# phpMyAdmin Configuration
PMA_HOST=mysql
PMA_PORT=3306
PMA_USER=root
PMA_PASSWORD=root_pass_2024_secure
PMA_ARBITRARY=1
PMA_UPLOAD_LIMIT=100M
PMA_MAX_EXECUTION_TIME=300
PMA_MEMORY_LIMIT=256M